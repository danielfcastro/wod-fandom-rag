# WoD Fandom RAG

Pipeline de coleta do Fandom (whitewolf.fandom.com) com indexação híbrida (OpenSearch + Qdrant), grafo leve em Neo4j e API/UX de QA.

## Subir os serviços
```bash
docker compose up -d --build

{ _ble_edit_exec_gexec__save_lastarg "$@"; } 4>&1 5>&2 &>/dev/null
